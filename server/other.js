//use letter timing 42.13
// const failed = true;
    
//     if(failed) return next(createError(401,"You are not authenticate"));


//Jsonweb token 
//by using cookies check is user is admin or not if user is not admin we send error and else user can update delete hotels